<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Activate your account</title>
    <style><%= stylesheet_link_tag 'email', media: 'all',  host: Rails.application.config.action_mailer.asset_host %></style>
  </head>
  <body>
    <div class="email-wrapper">
      <!-- Hero Section -->
      <div class="hero-section">
        <%= render 'email_templates/partials/hero', locals: { image_url:  'https://karya-app.s3.us-east-1.amazonaws.com/ChatGPT+Image+Sep+30%2C+2025%2C+11_40_57+PM.png' } %>
      </div>
        <div class="content">
        <%= render 'email_templates/partials/greeing', locals: { message: "Welcome, #{ @variables[:first_name] || 'there' }! 🎉" } rescue content_tag(:h1, "Welcome, #{ @variables[:first_name] || 'there' }! 🎉", class: 'greeting') %>
        
        <p class="welcome-message">
          We're thrilled to have you join the Karya community! You're just one step away from unlocking a world of possibilities and taking your productivity to the next level.
        </p>
        
        <%= render 'email_templates/partials/hightlight', locals: { message: "Why activate your account?</strong> Get instant access to all premium features, collaborate with your team, and start achieving your goals today." } rescue nil %>
        <%= render 'email_templates/partials/feature' %>
        
        <!-- CTA Button -->
        <div class="cta-section">
        <%= render 'email_templates/partials/button', locals: { href: @variables[:activation_url], text: 'Activate Your Account' } %>
        </div>
        
        <!-- Divider-->
        <%= render 'email_templates/partials/divider' %>
        
        <!-- Security Note -->
        <%= render 'email_templates/partials/security_note' %>
        
        <%= render 'email_templates/partials/need_help' %>
  </div>
  </body>
</html>